#include <stdio.h>
#include <stdlib.h>

int main()
{
    int primes[100] = {1,2,3}

    for(int i = 5; i <100; i + 2)
    {
        if(i% = 0)
        {
            array[i] = i;
        }
        else
            continue;
    }

    printf("Hello world!\n");


    return 0;
}
